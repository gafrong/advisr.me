<%= form_for @question do |f| %>  
  <p>
    <%= f.label :question %>
    <%= f.text_area :content %>  
  </p>
  <p>
    <%= f.label :category_id %>
    <%= f.collection_select :category_id, @categories, :id, :name %>
  </p>

  <button>Go</button>
<% end %>
